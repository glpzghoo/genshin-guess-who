import { GameBoard } from '@/components/game-board';

export default function GamePage() {
  return (
    <main className="min-h-screen bg-background pr-24 pl-24 pb-64">
      <GameBoard />
    </main>
  );
}
